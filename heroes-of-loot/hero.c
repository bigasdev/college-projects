#include <stdio.h>
#include "src/structs.h"
#include "src/hero.h"

extern struct App app;

void createHero(){
    //hero = &_hero;
}

void playerInputs(){
    if(app.up){
        printf("Going up!");
    }
}